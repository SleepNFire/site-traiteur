apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "site-traiteur.fullname" . }}-test-connection"
  labels:
    {{- include "site-traiteur.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "site-traiteur.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
